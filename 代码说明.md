# 代码文件说明（wechat-bridge 工程）

本文件从代码层面讲解 `wechat-bridge` 工程的每个文件、control.mjs 的模块与关键函数、以及数据流。
行号基于当前 `control.mjs`（约 1060 行）；函数行号为起始行，正文范围到下一个函数为止。

## 1. 文件总览

| 文件 | 作用 | 是否主链路 |
|---|---|---|
| `control.mjs` | **主服务（单文件）**：微信 iLink Bot + DSH RPC 客户端 + 消息路由 + 配置网页 + 同步推送 | ✅ 生产在用 |
| `index.html` | 配置网页：状态卡片、微信绑定、默认会话、会话列表、最近日志 | ✅ 随主服务提供 |
| `dsh-bridge.mjs` | [备用] Anthropic 兼容 `/v1/messages` → DSH RPC 的适配器，监听 3081 | ⭕ 独立进程版 |
| `wechat-bot.mjs` | [备用] 独立微信 Bot（默认对接 dsh-bridge.mjs） | ⭕ 独立进程版 |
| `package.json` | 脚本：`npm start` = `node control.mjs` | — |
| `config.json` | DSH 地址 + 微信登录态（botToken）+ 默认会话；权限 600 | 数据 |
| `state.json` | 微信发送凭证持久化（peerTokens / lastActivePeer），桥接重启不丢 | 数据 |
| `logs/control.{out,err}.log` | 运行日志 / 异常输出 | 数据 |

---

## 2. control.mjs 模块详解

### 2.1 启动与常量（1–28）
- 环境变量：`HOST/PORT`(3083)、`DSH_URL`(http://127.0.0.1:3080)、`TURN_TIMEOUT_MS`(10min)、`POLL_MS`(500ms)、`CONFIG_PATH`(./config.json)。
- `log()`：带时间戳写控制台 + 内存环形缓冲（`ring`，最多 300 条，供 `/api/logs` 读取）。

### 2.2 配置持久化（31–73）
- `loadConfig()` / `saveConfig()`：config.json 原子写入（tmp + rename + chmod 600）。
- `loadState()` / `saveState()`：state.json（发送凭证）读写——**每次收到微信消息都保存**，重启后同步推送能力不中断。

### 2.3 DSH 客户端（75–134）
- `dshRpc(method, payload)`：对 `{DSH_URL}/api/<method>` 发 `client-request`(带 rpcId)，校验 `server-response` 与 `result.ok`，成功返回 `result.value`。
- `dshStatus()`：用 `session.list` 探活 DSH。
- `listSessions()`：`session.list` 过滤已归档/空白，按最近活跃排序（与微信切换清单同源）。
- `extractText(content)`：从 assistant message 的 block 数组中提取 `type:"text"` 文本（过滤 reasoning）。
- `latestTurnNumber(sessionId)`：`session.history` 取当前最大 turn 号，作为"本任务之前"的基准。

用到的 DSH RPC：`session.list`、`session.create`、`session.prompt(mode:'queue')`、`session.cancel`、`session.history`、`workspace.list`。

### 2.4 优雅停机（136–144）
- `SIGTERM`：置 `shuttingDown=true`，让在途任务把「⚠️ 服务正在升级重启…」发回微信，8 秒宽限后强制退出。
- `SIGINT`：直接退出。
- 在途轮询（见 2.6）每轮检查 `shuttingDown`，返回中断通知而不是静默丢失。

### 2.5 同步推送核心状态（146–177）
- `HISTORY_WINDOW=200`：轮询/监听一次拉取最近 200 条事件（远大于 chunk 洪流速率，防阶段消息被挤出窗口）。
- `MAX_POLL_ERRORS=3`：连续 3 次 history 查询失败且持续 ≥4 秒 → 判定 DSH 失联。
- `sessionRelay`：**会话级共享 seq 游标** `{lastSeq, lastText}`——微信任务轮询与同步推送监听共用，保证每条回复只发一次。
- `engagedSessions`：微信任务正在处理某会话时，同步监听对该会话**让路**，杜绝双发。
- `peerTokens` / `lastActivePeer`：微信发送凭证与最近联系人（从收到的消息记录，已持久化）。
- `relayCursor(sessionId)` / `relayReasonText(reason)`：游标取用 / turn/end 原因→微信文案（error/aborted/interrupted 映射）。

### 2.6 核心等待链路：promptAndCollect / runPrompt / promptWithStatus（179–296）
`promptWithStatus`（268）→ `runPrompt`（257，按会话串行化）→ `promptAndCollect`（179）：

```
1. 记录 engaged：本会话交微信任务接管，同步监听让路
2. t0 = 当前最大 turn；session.prompt(mode:'queue') 排队
3. 循环（每 POLL_MS 500ms）：
   - isCancelled → 返回 ''（静默收尾，取消回执已发）
   - shuttingDown → 返回中断通知
   - session.history(200) → 按共享游标只处理新事件（turn > t0）
   - assistant/message 有文本：不同于已发文本 → onStage(txt) 立即发微信（阶段回复）
     发送成功才推进 deliveredText（失败留待最终回复补发，不丢失）
   - turn/end：记录 reason；hasEnd → 停止
   - 连续失败 ≥3 且 ≥4 秒 → 抛 dshDown 错误（外层发「DSH 服务似乎已停止或报错」）
4. hasEnd 后：取消→''；reason 映射错误/中断文案；否则返回 lastText（最终回复）
```

- 30 秒状态（266–267、268–296）：定时器每 10 秒检查一次，**距上次发到微信的消息 ≥30 秒**才发「⏳ 正在处理中…（已等待 N 秒）」，N 为**总等待时间**（从 `startAt` 起算）；任何发送（回执/阶段/状态）都重置计时。

### 2.7 回执（298–321）
- `sessionContextLabel`：查 workspace/session 标题，组成「工作区「x」 · 会话「y」」。
- `ackText`：默认「✅ 已收到，开始处理…」；微信渠道**超过 2 小时**（`IDLE_CONTEXT_MS`）没消息时附带上下文。

### 2.8 发送与微信 iLink 客户端（322–369）
- `sendTextWithRetry`（322）：`Promise.race(15s 超时)` + 2 次重试 + 2s 间隔，**全部失败抛错**（这是"失败即补发/重推"语义的根基）。
- `wxHeaders`（341）：`AuthorizationType: ilink_bot_token`、`X-WECHAT-UIN`(随机 uint32 base64)、`Authorization: Bearer <botToken>`。
- `wxPost`（350）：iLink 官方后端 `https://ilinkai.weixin.qq.com`。
- 用到的 iLink 端点：`ilink/bot/get_bot_qrcode`、`ilink/bot/get_qrcode_status`、`ilink/bot/getupdates`(长轮询)、`ilink/bot/sendmessage`。

### 2.9 绑定状态机（371–430）
- `startBind`：取二维码 → 轮询确认 → 拿到 `bot_token` 存 config.json。
- `doUnbind`：解绑（`bindGen` 使进行中的绑定流程失效）。

### 2.10 路由与微信命令（432–566）
- `resolveSession`：返回 `config.defaultSessionId`（单默认会话）。
- 命令常量：`切换对话`（别名 切换会话/切换到…）、`新对话`（别名 新建/新开…）、`CANCEL_WORDS=[取消,取消吧,停,停止,算了]`。
- 工具：`stripPunct`（去标点）、`isCmd`、`parseChoiceNum`（阿拉伯+中文数字 0–99）。
- `handleSwitchCmd` → `applySwitchChoice`：切换默认会话并持久化。
- `listWorkspaces` / `handleWorkspaceList` / `applyWorkspaceChoice`：「0」「新对话」→ 工作区清单 → `session.create` 新建会话。

### 2.11 发送文本与媒体处理（568–684）
- `sendText`（570）：组装 `sendmessage` 报文（`message_type:2`、`item_list:[{type:1,text_item}]`、`context_token`）。
- 媒体：图片/文件/视频（`MEDIA_TYPES={2,4,5}`）→ 下载 `media.full_url` → AES-128-ECB 解密（`parseAesKey`/`decryptAesEcb`，兼容 base64/hex 两种 key 编码）→ 按后缀/文件头识别类型 → 存 `当前会话工作区/wechat-media/<YYYYMMDD>/` → 回发保存路径。
- `sessionCwdOf`：通过 `workspace.list` 找会话所在工作区目录。

### 2.12 消息主入口 handleMessage（686–834）
```
message_type!=1 → 忽略（只处理用户消息）
提取 text（文本 / 语音 item.type=3 用 voice_item.text 平台转写）
记录 peerTokens + lastActivePeer + saveState()（同步推送凭证）
更新 lastMessageAt（供 2 小时空闲判断）；typing 1
[取消拦截] CANCEL_WORDS 且会话有 activeRun → run.cancelled=true
           + session.cancel + 「⏹ 已取消当前对话的执行。」
[命令分发] 切换对话 / 新对话 / 待选编号 / 待选取消
[普通消息] 默认会话为空→提示；否则：
   回执 → promptWithStatus(..., onStage=阶段直达, startAt=接收时刻)
   最终回复：reply===deliveredRef.current（已随阶段发出）→ 跳过；否则发送
            dshDown → 「DSH 服务似乎已停止或报错」；其他异常 → 「（DSH 调用失败：…）」
```

### 2.13 同步推送：watchForeignReplies / syncPush（836–911）
- `watchForeignReplies`：后台每 500ms 轮询**绑定（默认）会话**：
  - `engagedSessions` 命中 → 让路（微信任务正在跑）；
  - 首次关注某会话 → 游标初始化为当前最新 seq（**不推旧历史**）；
  - 新 `assistant/message` 文本 / `turn/end` 的错误·中断通知 → `syncPush` 推微信，
    失败则游标不动（下一轮重试），成功才推进——保证每条只发一次。
- `syncPush`（886）：冷却 30 秒（`SYNC_RETRY_MS`）内不重试、日志每 30 秒至多一条；
  无凭证时提示「请先在微信发一条消息」；成功记「同步推送已发送」。

### 2.14 微信长轮询 botLoop（913–942）
- 每轮 `getupdates`（带回 `get_updates_buf` 续传）；`errcode=-14` → 登录过期自动解绑；
- 消息**并发**处理（不 await，否则「取消」消息永远无法打断在途任务）。

### 2.15 HTTP 控制面（944–1060）
路由：`GET /`（网页）、`GET /api/status`、`GET /api/sessions`、`GET/POST /api/config`、`POST /api/bind|unbind`、`GET /api/logs`。
`statusPayload`（954）：DSH 可达性、Bot 状态、启动时长、消息/错误数。

---

## 3. 三条核心数据流

**A. 微信 → DSH（微信发起）**
```
微信消息 → getupdates → handleMessage → 回执 → promptWithStatus
  → runPrompt(会话串行) → promptAndCollect(session.prompt queue + 轮询 history)
  → 每步 assistant/message → onStage 立即发微信（阶段回复）
  → turn/end → 最终回复（与最后阶段相同则去重跳过）→ 回发微信
30 秒静默 → 「⏳ 正在处理中…（已等待 N 秒）」；执行中「取消」→ session.cancel
```

**B. 网页端 → 微信（同步推送）**
```
DSH 网页端在绑定会话发消息 → DSH 生成回复（assistant/message 事件落盘）
→ watchForeignReplies 轮询发现新事件（微信任务未占用该会话）
→ syncPush 逐条推送到最近联系人微信（含错误/中断通知）
```

**C. 部署重启（优雅停机）**
```
SIGTERM → shuttingDown=true → 在途轮询返回「⚠️ 服务正在升级重启，本条任务已被中断」
→ 消息发回微信 → 8 秒后退出 → launchd 拉起新进程（state.json 凭证已持久化）
```

---

## 4. 关键常量速查

| 常量 | 值 | 含义 |
|---|---|---|
| `HOST`/`PORT` | 127.0.0.1 / 3083 | 配置网页 |
| `DSH_URL` | http://127.0.0.1:3080 | DSH Web |
| `TURN_TIMEOUT_MS` | 10 min | 单轮回复等待上限 |
| `POLL_MS` | 500 ms | 轮询/监听间隔 |
| `HISTORY_WINDOW` | 200 | 每次拉取事件数 |
| `MAX_POLL_ERRORS` | 3（≥4 秒） | DSH 失联判定 |
| `WX_STATUS_INTERVAL_MS` / `CHECK_MS` | 30 s / 10 s | 状态提示 |
| `IDLE_CONTEXT_MS` | 2 h | 回执附带上下文阈值 |
| `SYNC_RETRY_MS` | 30 s | 同步推送失败冷却 |
| `MEDIA_TYPES` | {2:图片, 4:文件, 5:视频} | 媒体自动保存 |
| `ITEM_VOICE` | 3 | 语音（平台转写） |

## 5. 日志行解读

| 日志 | 含义 |
|---|---|
| `收到消息 [peer]: …` | 微信消息进来 |
| `阶段回复已发送 [peer]: …` | 微信任务的步骤文本已发 |
| `同步推送已发送 [peer]: …` | 网页端发起的回复已推微信 |
| `同步推送待发送：尚无微信发送凭证` | 重启后还没收到过微信消息，无法推送 |
| `同步推送失败（将自动重试）` | 30 秒后自动重试 |
| `已回复 [peer]: …` | 微信任务的最终回复已发 |
| `本轮无最终回复（已取消或已随阶段回复发出）` | 内容已发过或已取消，非异常 |
| `收到停止信号，进入优雅停机` | 优雅停机开始 |
| `微信登录过期，已自动解绑` | iLink errcode=-14，需重新扫码 |

## 6. index.html（配置网页）

单页：`<h1>DSH ↔ 微信 ClawBot 控制台</h1>`，区块依次为
状态卡片（DSH / 控制服务 / 微信 Bot）→ 微信绑定 → 默认会话 → 会话列表（15 秒自动刷新 + 立即刷新）→ 最近日志。
所有数据来自 §2.15 的 `/api/*` 接口，改动即时持久化。

## 7. 独立进程版（dsh-bridge.mjs / wechat-bot.mjs）

- `dsh-bridge.mjs`：把 Anthropic 兼容 `POST /v1/messages` 翻译成 DSH RPC（`session.prompt` 等），监听 3081；让任何 Anthropic 客户端可复用 DSH。
- `wechat-bot.mjs`：独立的扫码/长轮询 Bot，AI 接口默认指向 dsh-bridge；保留了"最小可拆"的架构，功能少于 control.mjs（无阶段回复/同步推送/媒体保存）。

## 8. 维护提示

- 改动 control.mjs 后：`node --check control.mjs` 再重启
  `launchctl kickstart -k gui/$(id -u)/com.macbot.dsh-wechat-bridge`（优雅停机保护在途任务）。
- 观察：`tail -f wechat-bridge/logs/control.out.log`。
- 自启：`~/Library/LaunchAgents/com.macbot.dsh-wechat-bridge.plist`（桥接）、`com.macbot.dsh-web.plist`（DSH web --no-open，带 3080 端口预检）。